<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.zane.mapper.DormBuildingMapper">

    <select id="findById" resultType="DormBuilding">
        SELECT id, name, status, created_at, updated_at
        FROM dorm_buildings
        WHERE id = #{id}
    </select>

    <select id="findByName" resultType="DormBuilding">
        SELECT id, name, status, created_at, updated_at
        FROM dorm_buildings
        WHERE name = #{name}
    </select>

    <select id="findEnabled" resultType="DormBuilding">
        SELECT id, name, status, created_at, updated_at
        FROM dorm_buildings
        WHERE status = 1
        ORDER BY id ASC
    </select>

    <select id="findAll" resultType="DormBuilding">
        SELECT id, name, status, created_at, updated_at
        FROM dorm_buildings
        ORDER BY status DESC, id ASC
    </select>

    <insert id="insert" parameterType="DormBuilding" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO dorm_buildings (name, status)
        VALUES (#{name}, #{status})
    </insert>

    <update id="update" parameterType="DormBuilding">
        UPDATE dorm_buildings
        SET name = #{name},
            status = #{status}
        WHERE id = #{id}
    </update>
</mapper>
