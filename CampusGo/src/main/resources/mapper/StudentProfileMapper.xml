<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.zane.mapper.StudentProfileMapper">

    <select id="findByUserId" resultType="StudentProfile">
        SELECT id, user_id, student_no, major, class_name, dorm_building, dorm_room
        FROM student_profiles
        WHERE user_id = #{userId}
    </select>

    <select id="countByStudentNo" resultType="int">
        SELECT COUNT(*)
        FROM student_profiles
        WHERE student_no = #{studentNo}
    </select>

    <insert id="insert" parameterType="StudentProfile">
        INSERT INTO student_profiles (user_id, student_no, major, class_name, dorm_building, dorm_room)
        VALUES (#{userId}, #{studentNo}, #{major}, #{className}, #{dormBuilding}, #{dormRoom})
    </insert>

    <update id="updateByUserId" parameterType="StudentProfile">
        UPDATE student_profiles
        SET major = #{major},
            class_name = #{className},
            dorm_building = #{dormBuilding},
            dorm_room = #{dormRoom}
        WHERE user_id = #{userId}
    </update>

</mapper>
