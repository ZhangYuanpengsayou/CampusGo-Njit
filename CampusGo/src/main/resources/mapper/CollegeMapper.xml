<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.zane.mapper.CollegeMapper">

    <select id="findById" resultType="College">
        SELECT id, name, description, status, created_at, updated_at
        FROM colleges
        WHERE id = #{id}
    </select>

    <select id="findByName" resultType="College">
        SELECT id, name, description, status, created_at, updated_at
        FROM colleges
        WHERE name = #{name}
    </select>

    <select id="findEnabled" resultType="College">
        SELECT id, name, description, status, created_at, updated_at
        FROM colleges
        WHERE status = 1
        ORDER BY id ASC
    </select>

    <select id="findAll" resultType="College">
        SELECT id, name, description, status, created_at, updated_at
        FROM colleges
        ORDER BY status DESC, id ASC
    </select>

    <insert id="insert" parameterType="College" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO colleges (name, description, status)
        VALUES (#{name}, #{description}, #{status})
    </insert>

    <update id="update" parameterType="College">
        UPDATE colleges
        SET name = #{name},
            description = #{description},
            status = #{status}
        WHERE id = #{id}
    </update>

    <delete id="delete">
        DELETE FROM colleges
        WHERE id = #{id}
    </delete>

</mapper>
